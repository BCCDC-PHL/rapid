{
    "lambda_args": [
	"filter_context"
    ],
    "required_arg_keys": {
	"filter_context": [
	    "directory",
	    "should_exist?"
	]
    },
    "lambda": "lambda x: if os.path.exists(x['directory']): return x['should_exist?']; else: return not x['should_exist?']"
}
