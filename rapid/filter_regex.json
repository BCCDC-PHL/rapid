{
    "lambda_args": [
	"filter_context"
    ],
    "required_arg_keys": {
	"filter_context": [
	    "pattern",
	    "string"
	]
    },
    "lambda": "lambda: filter_context: re.search(filter_context['pattern'], filter_context['string'])"
}
