{
    "lambda_args": [
	"filter_context"
    ],
    "required_arg_keys": {
	"filter_context": [
	    "greater_than",
	    "value",
	    "less_than"
	]
    },
    "lambda": "lambda x: x['greater_than'] < x['value'] and x['value'] < x['less_than']"
}
